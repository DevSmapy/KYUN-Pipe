# 🏠 House Prices - Advanced Regression Techniques

> Predict sales prices and practice feature engineering, RFs, and gradient boosting.
> Kaggle: https://www.kaggle.com/competitions/house-prices-advanced-regression-techniques

---

## 📌 1. Project Overview

- **Objective**: 79개의 설명 변수를 활용하여 아이오와주 에임스(Ames) 지역의 주택 가격을 예측하는 회귀 문제.
- **Data**: `train.csv`(label 포함), `test.csv`(label 없음)
- **Target**: `SalePrice`

---

## ✅ Submission Status

- **Kaggle Submission**: Completed
- **Final Model**: `<YOUR_FINAL_MODEL>` (e.g., LightGBM / XGBoost / RandomForest)
- **Leaderboard Score**
  - Public: `<PUBLIC_SCORE>`
  - Private: `<PRIVATE_SCORE>` (if available)
- **Submission Artifact**
  - File: `results/<run_id>/submission_<model>.csv` (generated by `ResultReporter`)

> Note: 제출 점수 및 최종 모델명은 리포트/메타데이터에 함께 고정 기록하는 것을 권장합니다.

---

## 🧪 Current Results (Local Validation)

> 아래는 `train.csv`만 사용한 hold-out 검증 결과입니다.

- **Metric (≈ Kaggle RMSLE)**: `0.13850` (RMSE on `log1p(SalePrice)` scale)
- **Reference (RMSE, original scale)**: `27121.50`
- **Validation**: `train_test_split(test_size=0.3, random_state=42)`
- **Baseline Model**: `RandomForestRegressor`
- **Preprocessing (high-level)**:
  - Target transform: `log1p(SalePrice)`
  - Missing (cat): `"None"` fill
  - Encoding: 일부 서열형 컬럼은 ordinal mapping, 나머지 범주형은 One-Hot
  - Missing (num): KNN imputation

➡️ 실험/변경 로그: [`results/Experiment_Report.md`](./results/Experiment_Report.md)
➡️ EDA 노트: [`results/EDA.md`](./results/EDA.md)

---

## ⚙️ 2. Pipeline Architecture

본 프로젝트는 `KYUN-Pipe` 코어 모듈을 활용하여 **재사용 가능한 전처리/학습 구조**로 구성됩니다.

1. **Data Loading**: `DataLoader`
2. **Preprocessing**: `UniversalPreprocessor` + `core/Transformers.py`
   - `ValueConverter` / `OrdinalMapper` / `ValueEncoder` / `NumericCaster`
3. **Training**: `ModelTrainer` (model별 `fit()` 시그니처 차이 안전 처리)
4. **Reporting**: `ResultReporter`
   - `metadata.json`, `metrics.json`, `submission.csv`, `<model>.pkl` 저장

---

## 🚀 3. How to Run

본 프로젝트는 `uv` 패키지 매니저를 사용합니다.

```bash
# 의존성 설치
uv sync
python competitions/c03_house_prices/main.py
```

## 💡 Key Focus Points

- **High-Dimensional Data**: 컬럼이 상당히 많기 때문에, EDA를 통해 각 변수의 중요도를 파악하고 불필요한 노이즈를 제거하는 과정에 집중합니다.
- **Advanced Regression**: 선형 회귀부터 복잡한 부스팅 모델까지 폭넓게 적용하고 최적의 하이퍼파라미터를 탐색합니다.
- **Consistent Pipeline**: `KYUN-Pipe`의 구조를 유지하며 데이터 전처리부터 모델 평가까지 유출(Leakage) 없는 실험 환경을 보장합니다.

## 📊 Results & Artifacts

실험 결과물은 실행 시점을 기준으로 `results/<run_id>` 폴더 내에 자동 아카이빙될 예정입니다.

| Artifact                 | Description                                   | Format |
| :----------------------- | :-------------------------------------------- | :----- |
| `metadata.json`          | 사용된 피처 리스트, 모델 하이퍼파라미터 정보  | JSON   |
| `metrics.json`           | RMSE, MAE, R2 Score 등 회귀 평가 지표         | JSON   |
| `submission_XGBoost.csv` | Kaggle 제출을 위한 최종 테스트 세트 예측 결과 | CSV    |
| `XGBoost.pkl`            | 학습된 모델 객체                              | Joblib |
